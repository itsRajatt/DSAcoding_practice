#include<bits/stdc++.h>
using namespace std;

int fact(int n)
{
    if(n==0 || n==1) return 1;
    return n*fact(n-1);
}
int zero(int n)
{
    int c=fact(n),ans=0;
    while(c%10==0)
    {
        ans++;
        c/=10;
    }
    return ans;
}
int main()
{
    cout<<zero(5);
    return 0;
}